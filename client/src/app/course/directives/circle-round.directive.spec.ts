import { CircleRoundDirective } from './circle-round.directive';

describe('CircleRoundDirective', () => {
  it('should create an instance', () => {
    const directive = new CircleRoundDirective();
    expect(directive).toBeTruthy();
  });
});
